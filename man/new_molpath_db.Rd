% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{new_molpath_db}
\alias{new_molpath_db}
\title{Create a new molpath_db object}
\usage{
new_molpath_db(
  patients = tibble::tibble(patient_id = character(), age = integer(), sex = character(),
    diagnosis = character()),
  samples = tibble::tibble(sample_id = character(), patient_id = character(), sample_type
    = character(), date = as.Date(character()), source_file = character()),
  variants = tibble::tibble(sample_id = character(), gene = character(), variant =
    character(), classification = character(), vaf = double()),
  reports = tibble::tibble(sample_id = character(), report_type = character(),
    report_date = as.Date(character()), summary_text = character(), source_file =
    character()),
  clinical = tibble::tibble(patient_id = character(), parameter = character(), value =
    character(), date = as.Date(character()), source = character()),
  survival = tibble::tibble(patient_id = character(), os_months = double(), os_status =
    integer(), pfs_months = double(), pfs_status = integer()),
  metadata = list(creation_date = Sys.time(), source_files = character(), parser_versions
    = list())
)
}
\arguments{
\item{patients}{A tibble with columns \code{patient_id}, \code{age}, \code{sex},
\code{diagnosis}, and optionally others.}

\item{samples}{A tibble with columns \code{sample_id}, \code{patient_id},
\code{sample_type}, \code{date}, \code{source_file}, and optionally others.}

\item{variants}{A tibble with columns \code{sample_id}, \code{gene}, \code{variant},
\code{classification}, \code{vaf}, and optionally others.}

\item{reports}{A tibble with columns \code{sample_id}, \code{report_type},
\code{report_date}, \code{summary_text}, \code{source_file}, and optionally others.}

\item{clinical}{A tibble with columns \code{patient_id}, \code{parameter}, \code{value},
\code{date}, \code{source}, and optionally others.}

\item{survival}{A tibble with columns \code{patient_id}, \code{os_months},
\code{os_status}, \code{pfs_months}, \code{pfs_status}, and optionally others.}

\item{metadata}{A list containing \code{creation_date} (POSIXct),
\code{source_files} (character vector), and \code{parser_versions} (named list).}
}
\value{
An S3 object of class \code{molpath_db}.
}
\description{
Constructs a \code{molpath_db} object, which is a list of linked tibbles
representing the core data model for molecular pathology workflows.
}
\examples{
db <- new_molpath_db()
db
}
