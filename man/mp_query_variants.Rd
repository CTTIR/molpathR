% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/queries.R
\name{mp_query_variants}
\alias{mp_query_variants}
\title{Filter variants in a molpath_db}
\usage{
mp_query_variants(
  db,
  genes = NULL,
  classification = NULL,
  min_vaf = NULL,
  max_vaf = NULL,
  variant_type = NULL
)
}
\arguments{
\item{db}{A \code{molpath_db} object.}

\item{genes}{Character vector of gene symbols to include. \code{NULL} means all.}

\item{classification}{Character vector of classifications to include.
\code{NULL} means all.}

\item{min_vaf}{Minimum variant allele frequency (inclusive). \code{NULL} for no
lower bound.}

\item{max_vaf}{Maximum variant allele frequency (inclusive). \code{NULL} for no
upper bound.}

\item{variant_type}{Character vector of variant types to include (e.g.,
\code{"SNV"}, \code{"Indel"}, \code{"CNV"}, \code{"Fusion"}). \code{NULL} means all.}
}
\value{
A tibble of matching variants.
}
\description{
Filter the variants table by gene, classification, VAF range, or variant
type.
}
\examples{
db <- mp_example_db(n_patients = 20, seed = 1)
mp_query_variants(db, genes = c("TP53", "KRAS"))
mp_query_variants(db, classification = "Pathogenic", min_vaf = 0.1)
}
