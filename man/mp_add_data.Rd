% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{mp_add_data}
\alias{mp_add_data}
\title{Add parsed data to an existing molpath_db}
\usage{
mp_add_data(db, parsed)
}
\arguments{
\item{db}{A \code{molpath_db} object to update.}

\item{parsed}{A single \code{molpath_parsed} object or a list of
\code{molpath_parsed} objects to add.}
}
\value{
An updated \code{molpath_db} object with the new data merged in.
}
\description{
Appends new data from one or more \code{molpath_parsed} objects to an existing
\code{molpath_db} database. Records are deduplicated after appending.
}
\examples{
\donttest{
db <- mp_example_db(n_patients = 5, seed = 1)
new_data <- new_molpath_parsed(
  data = tibble::tibble(patient_id = "P99", os_months = 12,
    os_status = 1L, pfs_months = 6, pfs_status = 1L),
  source_type = "survival", source_file = "new.csv"
)
db <- mp_add_data(db, new_data)
}
}
