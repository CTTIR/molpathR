% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{mp_plot_survival}
\alias{mp_plot_survival}
\title{Plot Kaplan-Meier survival curves}
\usage{
mp_plot_survival(db, group_by = NULL, type = c("os", "pfs"))
}
\arguments{
\item{db}{A \code{molpath_db} object.}

\item{group_by}{Optional character string. If it matches a column in the
patients table (e.g., \code{"diagnosis"}), stratify by that variable. If it
matches a gene name present in the variants table, stratify into
"Mutated" vs "Wild-type".}

\item{type}{Character; \code{"os"} for overall survival or \code{"pfs"} for
progression-free survival.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Creates Kaplan-Meier curves from the survival table. Optionally stratifies
by a clinical variable or gene mutation status.
}
\examples{
\donttest{
db <- mp_example_db(n_patients = 50, seed = 1)
mp_plot_survival(db, type = "os")
mp_plot_survival(db, group_by = "diagnosis", type = "os")
}
}
