% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{mp_validate_db}
\alias{mp_validate_db}
\title{Validate a molpath_db for referential integrity and completeness}
\usage{
mp_validate_db(db)
}
\arguments{
\item{db}{A \code{molpath_db} object to validate.}
}
\value{
Invisibly, a list with three elements:
\describe{
\item{valid}{Logical. \code{TRUE} if no referential integrity issues were found.}
\item{issues}{Character vector of issue descriptions (empty if valid).}
\item{completeness}{Named numeric vector with percentages of patients
having data in each table (e.g., \code{pct_with_variants},
\code{pct_with_survival}).}
}
}
\description{
Checks that all foreign key relationships in the database are satisfied
(e.g., every sample references an existing patient) and reports on data
completeness across tables.
}
\details{
The following referential integrity checks are performed:
\itemize{
\item All \code{patient_id} values in the samples table exist in patients.
\item All \code{sample_id} values in the variants table exist in samples.
\item All \code{sample_id} values in the reports table exist in samples.
\item All \code{patient_id} values in the clinical table exist in patients.
\item All \code{patient_id} values in the survival table exist in patients.
}

When called interactively, results are printed using \code{cli} formatting.
}
\examples{
\donttest{
db <- mp_example_db(n_patients = 10, seed = 1)
validation <- mp_validate_db(db)
validation$valid
}
}
