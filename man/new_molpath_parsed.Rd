% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{new_molpath_parsed}
\alias{new_molpath_parsed}
\title{Create a new molpath_parsed object}
\usage{
new_molpath_parsed(
  data = tibble::tibble(),
  source_type = character(1L),
  source_file = character(1L),
  parse_date = Sys.time()
)
}
\arguments{
\item{data}{A tibble (or data frame) containing the parsed data.}

\item{source_type}{A character string identifying the source type (e.g.,
\code{"vcf"}, \code{"xml_report"}, \code{"pdf_report"}, \code{"excel"}, \code{"csv"}).}

\item{source_file}{A character string with the path to the source file.}

\item{parse_date}{A \code{POSIXct} timestamp of when the data was parsed.
Defaults to the current time.}
}
\value{
An S3 object of class \code{molpath_parsed}.
}
\description{
Constructs a \code{molpath_parsed} object representing the result of parsing a
single data source. This serves as an intermediate representation before
data is integrated into a \code{molpath_db}.
}
\examples{
parsed <- new_molpath_parsed(
  data = tibble::tibble(gene = "BRAF", variant = "V600E"),
  source_type = "vcf",
  source_file = "sample1.vcf"
)
parsed
}
